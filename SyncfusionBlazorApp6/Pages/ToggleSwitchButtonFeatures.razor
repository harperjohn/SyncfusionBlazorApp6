@page "/toggleswitchbutton-features"
@using Syncfusion.Blazor.Buttons
<h2>Toggle Switch Button</h2>
<br />
<div id="ControlRegion">
    <div class="col-lg-12 control-section">
        <div class="content-wrapper">
            <div class="container switch-control">
                <div>
                    <label style="padding: 10px 70px 10px 0;">simple button</label>
                    <button @onclick="onYes">yes</button>
                </div>
                <div>
                    <label style="padding: 10px 70px 10px 0;">USB Tethering</label>
                    <SfSwitch @bind-Checked="@isUSB" @onkeypress="@onFocusx" @onfocus="onFocus" @onclick="onYes"></SfSwitch>
                </div>

               
                <div>
                    <label style="padding: 10px 70px 10px 0;">tech support test</label>
                    <SfSwitch @bind-Checked="@isChecked" ValueChange="change" TChecked="bool"></SfSwitch>
                </div>


                <div>
                    <label style="padding: 10px 24px 10px 0; ">Portable Wi-Fi hotspot</label>
                    <SfSwitch @onfocus="onFocus" @bind-Checked="@ishotspot"></SfSwitch>
                </div>
                <div>
                    <label style="padding: 10px 40px 10px 0; ">Bluetooth Tethering</label>
                    <SfSwitch Disabled="true" @bind-Checked="@isBluetooth"></SfSwitch>
                </div>
            </div>
        </div>
    </div>
</div>
<br />
<div>
    <h3>Selected Features:</h3>
    <ul class="ulstyle">
        <li class="list"> Default</li>
        <li class="list"> Theme - Bootstrap v4</li>
    </ul>
</div>
<br />
<style>
    .content-wrapper {
        margin-top: 47px;
    }

    .switch-control .heading {
        text-indent: 15px;
        padding: 10px 0;
        margin-left: -20px;
    }

    .switch-control div {
        display: flex;
        align-items: center;
    }

    .switch-control {
        margin: 0 auto;
        width: 275px;
    }

        .switch-control label {
            -webkit-tap-highlight-color: transparent;
            cursor: pointer;
            user-select: none;
            font-weight: 400;
            font-size: 13px;
        }

        .switch-control .e-disabled {
            pointer-events: none;
        }

    .list {
        float: left;
        line-height: 20px;
        margin: 10px;
        min-width: 200px;
    }

    .ulstyle {
        margin: 0px;
        padding-left: 20px;
        display: inline-block;
    }

    .list {
        float: left;
        line-height: 20px;
        margin: 10px;
        min-width: 200px;
    }
</style>
@code {
    private bool isUSB = true;
    private bool ishotspot = false;
    private bool isBluetooth = false;
    private bool isChecked = true;

    public void change (ChangeEventArgs<bool> args)
    {
        System.Console.WriteLine("tech support example toggle button");
    }

    private void onYes()
    {
        System.Console.WriteLine("in on yes");
    }
    private void onFocus(FocusEventArgs args)
    {
        System.Console.WriteLine("focusEventArgs version");
    }
    private void onFocusx(Microsoft.AspNetCore.Components.Web.KeyboardEventArgs args)
    {
        System.Console.WriteLine("focus");
    }
}|
